import React from 'react';
import { useHistory } from 'react-router-dom';

const Book = () => {
    const history = useHistory();

    return (
        <div>
            
        </div>
    );
};

export default Book;